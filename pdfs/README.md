# PDFs go here

When a guide is ready:

1. Save the PDF into this folder, e.g. `breathing.pdf`.
2. Open `app.js`, find that section, and set:
   - `pdf:`   to the file name (e.g. `"pdfs/breathing.pdf"`)
   - `ready:` to `true`

That's it — the card will open the PDF instead of "Coming soon".
